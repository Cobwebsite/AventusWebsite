:host {
	background-color: var(--primary-color);
	display: block;
	height: 50px;
	width: 100%;
	// position: fixed;
	// bottom: 0;

	.container {
		align-items: center;
		color: var(--primary-font-color);
		display: flex;
		height: 100%;
		justify-content: space-between;
		margin: auto;
		max-width: 1000px;
        font-size: 14px;
        padding: 0 20px;
        div {
            text-align: center;
        }
	}
}
